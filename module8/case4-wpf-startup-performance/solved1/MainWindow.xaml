<Window x:Class="StartupPerformance.Solved1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ERP System - Sistema Principal (Lazy Loading)" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        AutomationProperties.AutomationId="ERPMainWindow">
    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" Background="#34495E" Foreground="White" Height="35">
            <MenuItem Header="📁 Cadastros" Foreground="White" FontSize="14">
                <MenuItem Header="Clientes" Click="MenuClientes_Click"/>
                <MenuItem Header="Fornecedores" Click="MenuFornecedores_Click"/>
                <MenuItem Header="Produtos" Click="MenuProdutos_Click"/>
            </MenuItem>
            <MenuItem Header="💰 Vendas" Foreground="White" FontSize="14">
                <MenuItem Header="Nova Venda" Click="MenuNovaVenda_Click"/>
                <MenuItem Header="Pedidos" Click="MenuPedidos_Click"/>
            </MenuItem>
            <MenuItem Header="📊 Relatórios" Foreground="White" FontSize="14">
                <MenuItem Header="Vendas por Período" Click="MenuRelatorios_Click"/>
                <MenuItem Header="Produtos Mais Vendidos" Click="MenuRelatorios_Click"/>
            </MenuItem>
            <MenuItem Header="⚙️ Sistema" Foreground="White" FontSize="14">
                <MenuItem Header="Configurações" Click="MenuConfiguracoes_Click"/>
                <MenuItem Header="Sair" Click="MenuSair_Click"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Background="#34495E" Foreground="White" Height="30">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Sistema pronto - Lazy Loading ativado"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="ClientesCountText" Text="Clientes: não carregados"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Content Area -->
        <Grid x:Name="ContentArea" Background="#ECF0F1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🏢" FontSize="72" HorizontalAlignment="Center"/>
                <TextBlock Text="ERP System" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,10"/>
                <TextBlock Text="Selecione um menu para começar" 
                           FontSize="16" 
                           Foreground="#7F8C8D"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="✨ Inicialização rápida com Lazy Loading" 
                           FontSize="12" 
                           Foreground="#3498DB"
                           FontStyle="Italic"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
